create table project{
 name varchar(20),
 date of complition date,
 description varchar(50),
 };
 insert project(genderclass,02/06/2022,classification of gender);
 project desc;
 select * from project 
 order by date ;
 
